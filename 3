#include <iostream>
using namespace std;

void incredible_sort(int n, int k)
{
    int *arr = new int[n];
    
    for (int i; i < n; i++)
        cin >> arr[i];
    
    for (k = n; k != 0; k--)
        for (int i = 1; i < k; i++)
            if (arr[i - 1] > arr[i])
                swap(arr[i - 1], arr[i]);
    
    for (int i = 0; i < n; i++)
        cout << arr[i] << " ";
    
}

int main()
{
    int n, k; cin >> n >> k;
    incredible_sort(n, k);
    
    return 0;
}
